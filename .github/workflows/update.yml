name: 🚀 Auto Update File List

on:
  push:
    paths:
      - 'files/**'    # Запускается при изменении файлов в папке files
  workflow_dispatch:   # Позволяет запускать вручную

jobs:
  update-file-list:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: 🔍 Scan files and generate JSON
      run: |
        echo "🔄 Scanning files in /files directory..."
        
        # Создаем начальный JSON
        echo '[' > file-list-new.json
        
        # Перебираем все PDF файлы
        count=0
        for file in files/*.pdf; do
          if [ -f "$file" ]; then
            if [ $count -gt 0 ]; then
              echo "," >> file-list-new.json
            fi
            
            filename=$(basename "$file")
            size=$(stat -c%s "$file")
            modified=$(date -r "$file" +"%d.%m.%Y %H:%M")
            
            # Определяем описание по имени файла
            description="Документ"
            if [[ $filename == *"отчет"* ]] || [[ $filename == *"report"* ]]; then
              description="Финансовый отчет"
            elif [[ $filename == *"инструкц"* ]] || [[ $filename == *"manual"* ]]; then
              description="Инструкция пользователя"
            elif [[ $filename == *"презентац"* ]] || [[ $filename == *"presentation"* ]]; then
              description="Презентация проекта"
            elif [[ $filename == *"договор"* ]] || [[ $filename == *"contract"* ]]; then
              description="Юридический документ"
            fi
            
            # Форматируем размер файла
            if [ $size -gt 1048576 ]; then
              size_display=$(echo "scale=1; $size/1048576" | bc)" MB"
            elif [ $size -gt 1024 ]; then
              size_display=$(echo "scale=1; $size/1024" | bc)" KB"
            else
              size_display="$size B"
            fi
            
            # Создаем запись для файла
            cat >> file-list-new.json << EOF
            {
              "name": "$filename",
              "url": "files/$filename",
              "size": "$size_display",
              "modified": "$modified",
              "description": "$description"
            }
EOF
            count=$((count+1))
            echo "✅ Добавлен: $filename"
          fi
        done
        
        echo '' >> file-list-new.json
        echo ']' >> file-list-new.json
        
        # Форматируем JSON
        jq '.' file-list-new.json > file-list.json
        
        echo "🎉 Обработано файлов: $count"
        
    - name: 📊 Show changes
      run: |
        echo "=== Сгенерированный file-list.json ==="
        cat file-list.json
        echo "======================================"
        
    - name: 💾 Commit changes
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        # Проверяем есть ли изменения
        if git diff --quiet file-list.json; then
          echo "✅ Изменений нет"
        else
          git add file-list.json
          git commit -m "🤖 Auto-update: обновлен список файлов"
          git push
          echo "🚀 Изменения запушены"
        fi
